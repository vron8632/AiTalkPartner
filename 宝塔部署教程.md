# 🎙️ 言值AI - 智能口才陪练 · 宝塔面板保姆级上线教程

> 适用环境：宝塔面板（Nginx 1.28 / MySQL 5.7 / Python 3.11.4）
> 部署包结构：
> ```
> 言值AI部署包/
> ├── deploy.sh                    # ✅ 一键部署脚本（宝塔 Python 项目专用）
> ├── backend/                    # Django 后端（内含 React 构建产物 frontend_dist/，Django 统一服务前台+API+/admin）
> │   ├── manage.py
> │   ├── config/  users/  lessons/  practice/  payment/  e_mail/  services/
> │   ├── frontend_dist/          # ✅ 前端已经构建好，无需安装 Node
> │   ├── requirements.txt        # ✅ 已按 MySQL5.7 + Python3.11 降级：django==4.2.30
> │   └── .env.example            # 配置文件模板
> ├── frontend/                   # React 源码（备用，改版才需要，平时用不到）
> ├── database/
> │   └── yanzhi_database.sql     # ✅ 数据库备份（已转成 MySQL5.7 兼容格式）
> └── nginx.conf                  # Nginx 配置模板（见第三步）
> ```

---

## 一、准备工作：创建数据库

1. 把整个 **言值AI部署包** 上传到服务器 `/www/wwwroot/yanzhi/`（宝塔「文件」→ 上传 zip → 解压，得到 `/www/wwwroot/yanzhi/backend` 等目录）。
2. 打开宝塔 **phpMyAdmin**（软件商店 → phpMyAdmin → 设置 → 打开）。
3. 顶部点 **「数据库」→「新建数据库」**：
   - 数据库名：`karu_speech`
   - 排序规则：`utf8mb4_general_ci`（⚠️ 不要选 utf8mb4_0900_ai_ci，那是 MySQL8 专属，5.7 不支持）
4. 选中新建的 `karu_speech` 库 → 顶部 **「导入」** → 选择 `database/yanzhi_database.sql` → 执行。
5. 左侧能看到 18 张表（users_user、lessons_lesson、e_mail、payment_paymentorder...）即成功。

> 数据库里已包含：默认管理员（admin/admin123）、课程数据、用户练习记录。导入后可直接登录后台。

---

## 二、上传代码部署后端（宝塔「Python 项目」，不是 Python 项目管理器）

### 2.1 添加 Python 项目

1. 宝塔左侧菜单 **「网站」→「Python 项目」→「添加 Python 项目」**。
2. 填写：
   - 项目名称：`yanzhi`
   - 项目路径：`/www/wwwroot/yanzhi/backend`（必须精确到这个含 `manage.py` 的目录）
   - Python 版本：**3.11.4**（下拉选你已装的）
   - 启动方式：**Gunicorn**
   - 启动模块：`config.wsgi:application`
   - 运行端口：`8000`
   - 启动参数：`-b 127.0.0.1:8000 -w 2 --timeout 180`
   - 项目框架：Django
3. 提交后宝塔会自动创建虚拟环境并**自动执行 `pip install -r requirements.txt`**（如果没自动装，见 2.3 手动装）。

### 2.2 生成配置文件 .env

用宝塔「文件」打开 `/www/wwwroot/yanzhi/backend/`，把 **`.env.example` 复制一份改名为 `.env`**，然后编辑：

```ini
DJANGO_SECRET_KEY=随便填一串长随机字符（如 f3k9x2...，越乱越好）
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=你的域名

DB_NAME=karu_speech
DB_USER=root
DB_PASSWORD=你的宝塔MySQL密码
DB_HOST=localhost
DB_PORT=3306

# 第三方服务（不填则 AI 评价/语音识别不可用，可后补）
DEEPSEEK_API_KEY=
BAIDU_APP_ID=
BAIDU_API_KEY=
BAIDU_SECRET_KEY=
```

> ⚠️ `DB_PASSWORD` 填你**宝塔 MySQL 的 root 密码**（与 phpMyAdmin 登录密码一致）。

### 2.3 一键部署（宝塔「SSH 终端」执行）

> 部署包根目录自带 `deploy.sh`，一条命令完成：装依赖 → import 校验 → 建库 → 生成 .env → 迁移 → 静态文件 → 建管理员。

```bash
cd /www/wwwroot/yanzhi

# 执行一键部署（先填宝塔 MySQL root 密码和你的域名）
MYSQL_ROOT_PASSWORD='你的宝塔MySQL密码' DOMAIN='你的域名' bash deploy.sh /www/wwwroot/yanzhi/backend
```

按提示输入管理员账号/邮箱/密码即可。脚本输出 `✅ 部署完成` 后，回宝塔 → Python 项目 → `yanzhi` → **重启**。

> 想手动逐步执行的话，等价命令如下（了解原理用，可跳过）：
> ```bash
> cd /www/wwwroot/yanzhi/backend
> venv/bin/pip install -r requirements.txt
> venv/bin/python -c "import pymysql, rest_framework, rest_framework_simplejwt, djoser; print('import ok')"   # 防假安装
> venv/bin/python manage.py migrate
> venv/bin/python manage.py collectstatic --noinput
> venv/bin/python manage.py createsuperuser
> mkdir -p media/avatars media/audio && chown -R www:www media
> ```

### 2.4 验证后端

浏览器访问 `http://你的服务器IP:8000/` 应看到言值AI前台页面；`http://你的服务器IP:8000/admin/` 应能登录后台。
（IP 访问只是为了验证，正式入口走第三步的域名。）

---

## 三、上传代码部署前端、配置 Nginx

> **前端不需要部署服务器**：React 已经构建好放在 `backend/frontend_dist/`，由 Django 直接输出。
> 这一步实际是：**添加站点 + 配置 Nginx 反向代理到 Django**。

1. 宝塔 **「网站」→「添加站点」**：域名填你的真实域名（如 `www.example.com`），PHP 版本随便选（用不到），提交。
2. 站点设置 → **SSL** → 申请 **Let's Encrypt 证书**（需域名已解析到服务器 IP）。
3. 站点设置 → **配置文件** → 全选删除，粘贴 `nginx.conf` 模板内容（记得把里面所有 `your-domain.com` 替换成你的真实域名）→ 保存。
4. 站点设置 → **反向代理** 确认无冲突（本方案用配置文件直连 8000，不需要额外加反代）。

> 若你的宝塔证书路径与模板不同：以站点设置里 SSL 页显示的「证书文件/密钥文件」路径为准，替换模板中 `ssl_certificate` 两行。

## 四、重启 Nginx

宝塔首页 → **「重启」→「重启 Nginx」**（或右侧 Nginx 重启按钮）。

然后访问 `https://你的域名/`：
- ✅ 进入言值AI 前台（课程、练习、评价、登录注册）
- ✅ `https://你的域名/admin/` 进入 Django 管理后台
- ✅ `/api/...` 接口自动可用

---

## 常见问题（FAQ）

| 现象 | 原因与解决 |
|---|---|
| 502 Bad Gateway | gunicorn 没起来：宝塔 Python 项目 → yanzhi → 日志查看报错；确认端口 8000 在监听（`ss -tlnp \| grep 8000`） |
| 页面能开但录音/上传失败 | ① 必须 HTTPS（getUserMedia 要求）；② `client_max_body_size 50m` 是否已配 |
| admin 登录提示密码错误 | 用 2.3 步骤创建的 superuser 登录 |
| 注册时发不了验证码 | 后台「邮箱配置」改真实 SMTP（smtp.qq.com / 465 / 邮箱 / 授权码） |
| AI 评价转圈后报错 | `backend/.env` 的 `DEEPSEEK_API_KEY` / `BAIDU_*` 未填或填错 |
| pip 装 pymysql 报错/import 失败 | 确认用的是 `venv/bin/pip` 和 `venv/bin/python`（宝塔系统 python 与项目 venv 是两个环境，最容易在这里"假安装"） |

## 版本兼容说明（已为你优化）

- **Django 5.2 → 4.2.30**（LTS）：兼容 Python 3.11.4 + MySQL 5.7，生态最稳
- **django-simpleui==2026.1.13**（原 2026.1 版本号不存在）
- **social-auth-app-django==5.4.0**：djoser 的依赖，锁 5.4 以兼容 Django 4.2（新版 5.9 要求 Django≥5.2，会"假安装成功"）
- **数据库 SQL**：已把 MySQL8 的 `utf8mb4_0900_ai_ci` 全部转为 `utf8mb4_general_ci`，5.7 可直接导入
